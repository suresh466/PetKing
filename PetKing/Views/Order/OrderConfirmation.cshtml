@model PetKing.Models.Order
<div class="container mt-5 order-confirmation">
    <div class="card border-0 shadow">
        <div class="card-body p-4 p-md-5">
            <h2 class="card-title text-center mb-4">Order Confirmation</h2>
            <div class="row">
                <div class="col-md-6 mb-4 mb-md-0">
                    <div class="card h-100">
                        <div class="card-body">
                            <h4 class="card-title">Order Details</h4>
                            <p class="mb-1"><strong>Order Number:</strong> #@Model.OrderID</p>
                            <p class="mb-1"><strong>Order Date:</strong> @Model.OrderDate.ToString("MMMM dd, yyyy HH:mm")</p>
                            <p class="mb-1"><strong>Total Amount:</strong> @Model.TotalAmount.ToString("C")</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card h-100">
                        <div class="card-body">
                            <h4 class="card-title">Shipping Information</h4>
                            <p class="mb-1"><strong>Name:</strong> @Model.ShippingName</p>
                            <p class="mb-1"><strong>Address:</strong> @Model.ShippingAddress</p>
                            <p class="mb-1"><strong>City:</strong> @Model.ShippingCity</p>
                            <p class="mb-1"><strong>Postal Code:</strong> @Model.ShippingPostalCode</p>
                        </div>
                    </div>
                </div>
            </div>
            <div class="mt-4">
                <h4 class="mb-3">Order Items</h4>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead class="thead-light">
                            <tr>
                                <th>Product</th>
                                <th>Quantity</th>
                                <th>Price</th>
                                <th>Subtotal</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.OrderItems)
                            {
                                <tr>
                                    <td>@item.Product.Name</td>
                                    <td>@item.Quantity</td>
                                    <td>@item.Price.ToString("C")</td>
                                    <td>@((item.Quantity * item.Price).ToString("C"))</td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr>
                                <td colspan="3" class="text-right"><strong>Total:</strong></td>
                                <td><strong>@Model.TotalAmount.ToString("C")</strong></td>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
            <div class="text-center mt-4">
                <a asp-controller="Product" asp-action="Index" class="btn btn-primary">Continue Shopping</a>
            </div>
        </div>
    </div>
</div>